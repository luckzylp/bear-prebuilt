name: Bear Auto Prebuilt

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  BEAR_REPO: rizsotto/Bear

jobs:
  get-version:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.get-tag.outputs.tag }}

    steps:
      - name: Get latest Bear release tag
        id: get-tag
        shell: bash
        run: |
          LATEST_TAG=$(curl -fsSL \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/rizsotto/Bear/releases/latest \
            | jq -r .tag_name)
          echo "tag=$LATEST_TAG" >> "$GITHUB_OUTPUT"
          echo "Latest Bear version: $LATEST_TAG"

  build:
    needs: get-version
    runs-on: ${{ contains(matrix.target, 'windows') && 'windows-latest' || 'ubuntu-latest' }}

    strategy:
      fail-fast: false
      matrix:
        target:
          # Linux (glibc high)
          - x86_64-unknown-linux-gnu
          - aarch64-unknown-linux-gnu
          - armv7-unknown-linux-gnueabihf
          - i686-unknown-linux-gnu

          # Linux (glibc 2.17)
          - x86_64-unknown-linux-gnu.2.17
          - aarch64-unknown-linux-gnu.2.17
          - armv7-unknown-linux-gnueabihf.2.17
          - i686-unknown-linux-gnu.2.17

          # Windows (MSVC)
          - x86_64-pc-windows-msvc
          - aarch64-pc-windows-msvc

          # macOS
          - x86_64-apple-darwin
          - aarch64-apple-darwin

    steps:
      - name: Checkout Bear prebuilt repo
        uses: actions/checkout@v4
        with:
          path: bear-prebuilt

      - name: Checkout Bear
        uses: actions/checkout@v4
        with:
          repository: ${{ env.BEAR_REPO }}
          ref: ${{ needs.get-version.outputs.tag }}
          path: bear-prebuilt/Bear

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ contains(matrix.target, 'windows') && 'x86_64-pc-windows-msvc,aarch64-pc-windows-msvc' || '' }}

      - name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Install cargo-zigbuild
        if: "!contains(matrix.target, 'windows')"
        run: cargo install cargo-zigbuild

      - name: Add Rust target (Linux/macOS)
        if: "!contains(matrix.target, 'windows')"
        run: |
          TARGET="${{ matrix.target }}"
          TARGET="${TARGET%.2.17}"
          rustup target add "$TARGET"

      - name: Add Rust target (Windows)
        if: contains(matrix.target, 'windows')
        run: pwsh -Command "'${{ matrix.target }}'.Replace('.2.17', '') | ForEach-Object { rustup target add $_ }"

      - name: Apply platform-specific patches
        working-directory: bear-prebuilt
        shell: bash
        run: |
          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            echo "Applying Windows patches..."
            bash scripts/patch-build-rs-windows.sh
          elif [[ "${{ matrix.target }}" == *"darwin"* ]]; then
            echo "Applying macOS patches..."
            bash scripts/patch-build-rs-macos.sh
          else
            echo "Applying Linux patches..."
            bash scripts/patch-build-rs-linux.sh
          fi

      - name: Build Bear (zigbuild)
        if: "!contains(matrix.target, 'windows')"
        working-directory: bear-prebuilt/Bear
        run: |
          cargo zigbuild --release --target ${{ matrix.target }}

      - name: Build Bear (MSVC)
        if: contains(matrix.target, 'windows')
        working-directory: bear-prebuilt/Bear
        run: |
          cargo build --release --target ${{ matrix.target }}

      - name: Install NSIS (Windows)
        if: contains(matrix.target, 'windows')
        run: choco install nsis -y

      - name: Install dpkg tools (Linux)
        if: "!contains(matrix.target, 'windows') && !contains(matrix.target, 'darwin')"
        run: sudo apt-get update && sudo apt-get install -y dpkg-dev fakeroot

      - name: Create Windows Installer
        if: contains(matrix.target, 'windows')
        working-directory: bear-prebuilt
        run: pwsh scripts/create-windows-installer.ps1 -Version "${{ needs.get-version.outputs.tag }}" -TargetTriple "${{ matrix.target }}"

      - name: Create Debian Package
        if: "!contains(matrix.target, 'windows') && !contains(matrix.target, 'darwin')"
        working-directory: bear-prebuilt
        run: bash scripts/create-deb-package.sh "${{ needs.get-version.outputs.tag }}" "${{ matrix.target }}"

      - name: Create macOS Package
        if: contains(matrix.target, 'darwin')
        working-directory: bear-prebuilt
        run: bash scripts/create-macos-dmg.sh "${{ needs.get-version.outputs.tag }}" "${{ matrix.target }}"

      - name: Package (Linux/macOS) - Legacy
        if: "!contains(matrix.target, 'windows')"
        working-directory: bear-prebuilt
        run: bash scripts/package.sh "${{ needs.get-version.outputs.tag }}" "${{ matrix.target }}"
        continue-on-error: true

      - name: Package (Windows) - Legacy
        if: contains(matrix.target, 'windows')
        working-directory: bear-prebuilt
        run: pwsh scripts/package.ps1 "${{ needs.get-version.outputs.tag }}" "${{ matrix.target }}"
        continue-on-error: true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: bear-prebuilt/dist/*

  release:
    needs: [get-version, build]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: bear-${{ needs.get-version.outputs.tag }}
          name: Bear ${{ needs.get-version.outputs.tag }} (Prebuilt)
          body: |
            Automated prebuilt binaries and installers for Bear ${{ needs.get-version.outputs.tag }}

            ## Installation Paths
            * **Windows**: `C:\Program Files\Bear\` (via .exe installer)
            * **Linux**: `/usr/lib/libexec/bear/` (via .deb package)
            * **macOS**: `/usr/lib/libexec/bear/` (via .pkg installer)

            ## Features
            * Built with cargo + zigbuild
            * Linux: glibc high & glibc 2.17
            * Linux x64: Includes multilib support (x86_64 + i386 preload libraries)
            * Windows: NSIS installer with automatic PATH configuration
            * macOS: Native .pkg installer
            * All platforms: Platform-specific installation paths configured
          files: dist/**

      - uses: actions/checkout@v4

      - name: Save built tag
        run: |
          echo "${{ needs.get-version.outputs.tag }}" > last_built_tag
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add last_built_tag
          git commit -m "chore: built Bear ${{ needs.get-version.outputs.tag }}"
          git push
